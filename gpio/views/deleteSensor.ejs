
<div id="deleteSensorModal"
     class="modal fade"
     tabindex="-1"
     role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">

      <div class="modal-header modal-header-danger">
        <button type="button"
                class="close"
                data-dismiss="modal"
                aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <h4 class="modal-title">{{ label.deleteSensor_modalTitle }}</h4>
      </div>

      <div class="modal-body">
        <br/>
        <p>{{ label.deleteSensor_message }} <strong> {{ sensorData.sensorNumber }} </strong> !!! &hellip;</p>
      </div>

      <div class="modal-footer">
        <button type="button"
                class="btn btn-default"
                data-dismiss="modal">
          {{ label.deleteSensor_close }}
        </button>
        <button type="button"
                class="btn btn-primary"
                data-ng-click="deleteSensorType()">
          {{ label.deleteSensor_delete }}
        </button>
      </div>

    </div><!-- /.modal-content -->
  </div><!-- /.modal-dialog -->
</div><!-- /.modal -->
